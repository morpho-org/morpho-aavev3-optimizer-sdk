import { BigNumber } from "ethers";

export const MAX_UINT_160 = BigNumber.from("0x" + "f".repeat(40));

export const MAX_UINT_48 = BigNumber.from("281474976710655");

export const LT_LOWER_BOUND = BigNumber.from(10_00);
